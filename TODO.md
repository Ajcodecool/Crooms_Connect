- [ ] Apply username/date/type filters from the main chat feed in src/pages/Chat.jsx
- [ ] Import needed filter utilities (usernameFilter) and use them to validate usernames client side where appropriate
- [ ] Add state + memoized filtering logic so blocked users + user/date/type filters are applied to visible messages
- [ ] Run lint/build or TypeScript check (if available)

